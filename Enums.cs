namespace BridgePointsCounter
{
    class Enums
    {
        public enum Doubles{ undoubled, doubled, redoubled };
        public enum Colors { clubs, diamonds, hearts, spades, notrumps };
    }
}
